version: '3.9'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react_app
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
